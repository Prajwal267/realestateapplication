import React from 'react'
import '../styles/home.css'
export const Home = () => {
  return (
    <div>
        
    </div>

  )
}
export default Home;  // export default Home; is used to export the component so that it can
